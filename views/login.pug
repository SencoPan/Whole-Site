extends authLayout

block links
    link(rel='stylesheet' type='text/css' href='../stylesheets/forOptionalAuth.css')

block form
        form(method="post" class="registration disabled probPass" action='auth/login')
            h1 Authorization

            label Login
            input(type="text" name="login")

            label Password
            input(type="password" name="password")

            input(type='submit' value='Log in' class="active")
        .option-span
            span either register with those
        .other-option
            a(href="/auth/google")
                .block-for-options
                    p Sign in using Google+ &nbsp;
                        i.fab.fa-google-plus-square
            a(href="/auth/git")
                .block-for-options
                    p Sign in using GitHub &nbsp;
                        i.fab.fa-github-square
            a(href="/auth/")
                .block-for-options
                    p Sign in using Facebook &nbsp;
                        i.fab.fa-facebook-square